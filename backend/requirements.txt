# Backend dependencies, pinned to versions verified working together
# locally (Python 3.11). torch/torchvision are installed from the CPU-only
# wheel index in the Dockerfile (--index-url https://download.pytorch.org/whl/cpu)
# so the deployed image doesn't pull CUDA libraries it'll never use on
# Render's CPU-only free tier -- do not add a CUDA-specific version suffix
# here, the Dockerfile's install command handles that.

fastapi==0.135.1
uvicorn[standard]==0.35.0
pydantic==2.11.5
pydantic-settings==2.10.1
python-dotenv==1.2.1
sqlalchemy[asyncio]==2.0.51
asyncpg==0.30.0
alembic==1.18.3
psycopg2-binary==2.9.9  # sync driver, used only by Alembic migrations (see app/alembic/env.py)
python-multipart==0.0.22

# Image processing / classical CV features
opencv-python-headless==4.12.0.88
numpy==2.1.3
pillow==12.2.0
scipy==1.16.2

# ML
torch==2.5.1
torchvision==0.20.1
scikit-learn==1.7.2
joblib==1.5.1
