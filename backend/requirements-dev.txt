# Test-only dependencies, on top of requirements.txt. Not installed in the
# production Docker image (backend/Dockerfile only installs requirements.txt).
-r requirements.txt
pytest==8.4.2
pytest-asyncio==1.2.0
httpx==0.28.1
