"""
Application settings.

Will define a pydantic-settings BaseSettings subclass reading configuration
from environment variables (Neon Postgres pooled connection URL, Alembic's
direct connection URL, CORS allowed origins, ML model weights path, upload
size/type limits). Not implemented yet.
"""
