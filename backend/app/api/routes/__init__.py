"""
API route modules.

Will hold the FastAPI routers for: image upload + analysis (accepts an
image, validates it, runs the CV/ML pipeline, persists and returns the
structured result), analysis history retrieval, and a health/status check.
Not implemented yet.
"""
