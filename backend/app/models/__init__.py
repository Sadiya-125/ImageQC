"""
SQLAlchemy ORM models.

Will define the persisted analysis-result model (uploaded image reference,
quality_score, quality_label, per-issue detections with severity/confidence,
classical image statistics, timestamps) used to satisfy the "persist and
retrieve previous analyses" backend requirement. Not implemented yet.
"""
