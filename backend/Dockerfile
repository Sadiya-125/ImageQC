# Placeholder Dockerfile for the FastAPI backend.
#
# To be implemented once backend routes, dependencies, and the exported ML
# model are finalized. Target: Python 3.11+, CPU-only torch/torchvision
# (install from https://download.pytorch.org/whl/cpu to keep the image
# small), deployed to Render as a Docker web service (see render.yaml and
# BUILD_SPEC.md).
